#include <stdio.h>
#include <stdlib.h>

#include "Head.h"
int main()
{
    int x = 10;
    int y = 20;
    printf("Sum of %d & %d = %d\n",x,y,sum_values(x,y));
    printf("Muliply_values of %d & %d = %d\n",x,y,Muliply_values(x,y));
    return 0;
}
